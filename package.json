{
  "name": "rewe-score",
  "version": "0.0.1",
  "description": "Form for the recipe input and creation of a .csv document.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Torben H. Pfohl",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
